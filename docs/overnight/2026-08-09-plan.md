# Documake — Overnight Development Plan

Sat 2026-08-08 23:57 → Sun 2026-08-09 08:00 CDT · framework: CONTRACT/FENCE/ISOLATE/ORIENT/REALITY/PLAN/LOOP/BAR/FLEET/HANDOFF

## CONTRACT

- Unattended window: **00:15 → T = 08:00 CDT**. Pencils down at T regardless of state.
- Mode: decide–record–move-on. Every judgment call gets one line in `docs/overnight/2026-08-09-decisions.md` (what, why, cost to reverse). No decision may block longer than 5 minutes.
- Nothing merges, nothing is final: Artem reviews the diff in the morning and owns merge/discard.

## FENCE

- All work on branch **`nightly/2026-08-09`** in a dedicated worktree. `main` holds tonight's baseline commit of v1 and is never touched again.
- **No push** (no remote exists; none gets added). **No merge to main. No deploy/publish** (no `vercel`, no `npm publish`).
- **No secrets**: never read other projects' `.env`; the render-API bearer token is an optional env var documented in `.env.example` only; secret-scan the diff before every commit.
- **No dependency majors.** Target zero new runtime deps; a new dep requires "task impossible without it," pinned version, decisions-log entry.
- **No fake content** — Documake-specific rules:
  - Sample brand profiles are labeled "Sample" in name and UI.
  - No invented prices anywhere: Weblux's own pricing is an explicitly open business decision (per 2026-08-08 ecosystem research). SOW/quote money slots default blank/"TBD".
  - No new legal boilerplate beyond the existing NDA pattern; the not-legal-advice disclaimer stays on every legal template.
- Other repos (`central.hub`, `weblux*`, `dave`, `crm`, `lawai*`, `ava`) are **read-only reference**. Never modified, never imported from — brand token *values* may be copied with a source comment.

## ISOLATE

- `~/documake` has no git yet → phase 0: `git init` + baseline commit on `main`, then `git worktree add ~/documake-nightly nightly/2026-08-09`. All edits happen in the worktree; `~/documake` stays pristine.
- Dev/verify server on **port 4310**: port 3000 is held by central.hub's docker container, and a second unattended run (affordable-refrigeration) is active on this machine tonight.
- Shared-machine discipline: max 3 concurrent subagents, no `pkill`/global process operations, no ports in the 3000/8000 ranges.
- Disposable data only: vitest fixtures; browser localStorage under the :4310 origin is scratch.

## ORIENT (≤45 min, target 25)

- Re-read Documake's own: README, AGENTS.md (Next 16 docs live in `node_modules/next/dist/docs` — check the route-handler guide before building the render API), `src/lib/*`, `src/components/pdf/theme.ts`, the three PDF templates.
- Read-only reference pulls: weblux `docs/onboarding/templates/map.md` + `04-proposal-and-agreement.md` (Map/quote/SOW section specs); weblux `src/app/globals.css` + weblux-hf `app/design-brief.md` (brand palette + type rules); dave `packages/amber-ui/src/styles/ember.css` (ember tokens).
- Verify against installed source, not memory: `@react-pdf/renderer` server-side `renderToBuffer` in the installed version; zod 4 API for any new schema features.

## REALITY (assessed 23:50; re-verify at 00:15)

- central.hub carries uncommitted WIP on two worktrees → **hub division integration is OUT of tonight's scope**; it becomes a written proposal for a joint session instead.
- weblux carries uncommitted onboarding WIP → read-only, copy values not files.
- Second overnight run on this machine → resource/port rules above; keep fleet small.
- Documake baseline was gate-green at 23:15 (build ✓, 44 tests ✓, browser-verified). **Early-exit clause:** if baseline gates are red at 00:15 and unfixable in 30 min, tonight converts to an audit/fix-only night.

## PLAN — ranked backlog (value × unattended-confidence ÷ effort)

| # | Task | V | C | E | Score | Box |
|---|------|---|---|---|-------|-----|
| T1 | Storage repository seam: adapter interface over localStorage, schema-versioned persistence (prep for future SQLite/hub backend) | 6 | .95 | 1 | 5.7 | 40m |
| T2 | **Multi-brand profiles**: `profiles[]` + active-profile id, v1→v2 storage migration (tested), profile switcher UI, per-profile identity + branding fields | 9 | .85 | 3 | 2.6 | 90m |
| T3 | **PDF brand theming**: per-profile theme tokens through `theme.ts`; two labeled sample presets (Weblux-style ink/serif `#002b72` + Instrument Serif; Ember-style cognac `#bc4b00`); font embedding with Helvetica fallback if font files unavailable offline | 8 | .8 | 2 | 3.2 | 60m |
| T4 | **Quote/Proposal template**: schema + form + PDF + tests; line items reusing invoice-math, validity date, acceptance line, terms slots | 8 | .9 | 2.5 | 2.9 | 75m |
| T5 | **Stateless render API** `POST /api/render`: zod-validated `{type, data, profile}` → PDF stream; optional `Authorization: Bearer $DOCUMAKE_RENDER_SECRET` (hub `CRON_SECRET` pattern, off when unset); integration tests | 7 | .9 | 2 | 3.2 | 45m |
| T6 | **SOW template**: scope/deliverables/milestones/payment-schedule (40/40/20-shaped slots, all amounts TBD), assumptions, change-control | 7 | .7 | 2.5 | 2.0 | 60m |
| T7 | *Stretch* — "The Map" report template (weblux 3-table structure from `map.md`) | 8 | .6 | 4 | 1.2 | 60m |
| T8 | Hub-division integration RFC (markdown only, no hub code) — written during handoff | 4 | .95 | .5 | — | 20m |

Execution order resolves dependencies (T1→T2→T3→T4→T5→T6→T7). Each task is independently shippable — the night is a success even if it ends after T3.

**Timeline (CDT):** 00:15 phase 0 · 00:40 T1 · 01:20 T2 · 02:50 T3 · 03:50 T4 · 05:05 T5 · 05:50 T6 · 06:50 T7-or-buffer · 07:30 final gates + handoff · 08:00 down. Any task 50% over its box → decide, record, park.

## LOOP (per task)

1. **Research** — read target files + installed-package docs; no memory-coding.
2. **Plan** — 5-line task plan appended to the decisions log.
3. **Test** — failing vitest first for all logic (migration, math, schema, API).
4. **Build** — house rules: small files, immutable updates, zod at boundaries, no hardcoded values.
5. **Verify** — `npx tsc --noEmit` + `npx vitest run` + `npm run build` + browser pass on :4310 with screenshot archived to `docs/overnight/evidence/` and clean console.
6. **Commit** — one task = one conventional commit on the nightly branch. No AI-attribution trailers.

## BAR

- Standard: **"I'd ship it to a Weblux client Monday."** Less than that gets fixed or reverted — no WIP left on the branch.
- **3 strikes** per task (strike = verify gate still red after a genuine fix attempt) → reset the task's commits, write findings to `docs/overnight/2026-08-09-parked.md`, move on.
- Never weaken or delete a test to get green; fix the implementation.

## FLEET

- **Orchestrator + judge: Fable** (main loop) — owns the decisions log, runs every gate itself, makes every commit, performs adversarial diff review. No subagent claim is trusted unverified.
- **Build & debug lanes: Opus subagents** working in the worktree — one task at a time, max 2 concurrent only when truly independent (e.g., T4 form work alongside T5 API).
- **Sweeps: Sonnet subagents** — post-milestone conformance passes (immutability, file size <800, hardcoded-value/secret scan of the diff, validation-at-boundaries) and a test-gap sweep after T4.
- **Judge gates** at 03:50 (post-T3) and 07:30 (full diff): fable-level adversarial review — ship-bar, fence compliance, fake-content scan.

## HANDOFF (07:30–08:00, or at early exit)

- `docs/overnight/2026-08-09-report.md` — written **as-you-go**, per task: what/why/evidence/commit.
- `docs/overnight/2026-08-09-morning.md` — a 5-minute morning script: start dev on :4310 → switch profiles → create a quote → download PDF → one `curl` against the render API; then `git log --oneline main..nightly/2026-08-09`, `git diff main...nightly/2026-08-09 --stat`, and merge instructions (Artem's call; merging is outside the fence).
- End state: nightly branch **gate-green**, evidence folder populated, `main` untouched, worktree intact.
- Early-exit: phase-0 red >30 min, or two consecutive parked tasks → stop building, write honest audit findings, end with a shorter report. Nothing worth doing beats fake progress.
